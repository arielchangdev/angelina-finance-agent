---
- name: Reload systemd for caddy
  ansible.builtin.systemd:
    daemon_reload: true

- name: Reload caddy
  ansible.builtin.systemd:
    name: caddy
    state: reloaded
  # If caddy isn't running yet on a first run, the enable+start task handles it.
  failed_when: false
